// Collections of value

// Empty array
let students = [];

// an array of strings
let colors = ["red", "orange", "yellow"];

// An array of nos
let numArr = [1, 2, 3, 4, 5];

// mixed array
let waste = [true, undefined, 12, 9.999, NaN, false, null, "HIIIII"];

console.log(typeof numArr); // object

// every array has length i.e number of char present
console.log(numArr.length);
